spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:musicdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show-sql: false
        format-sql: false
